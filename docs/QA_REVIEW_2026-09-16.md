**QA SignalGate — 16 September 2026**

Kesimpulan: fondasi pengujian sudah berjalan, tetapi ada tiga masalah prioritas tinggi pada antrean, penyimpanan hasil, dan asosiasi dokumen. Perbaiki ketiganya sebelum memakai hasil scan sebagai alur demo utama atau membuka penggunaan lebih luas. Status kesiapan fungsional: **DO NOT SHIP sebelum P1 diperbaiki**. Ini penilaian reliabilitas aplikasi, bukan penilaian akurasi finansial model.

Review mencakup kode kerja saat ini, termasuk perubahan Claude Code yang belum di-commit. Kode aplikasi tidak diubah. Pengujian browser menggunakan salinan database lokal, direktori kerja sementara, Sectors dimatikan, model dimatikan, dan pengambilan sumber dalam mode offline.

Pemeriksaan yang dijalankan:

- Backend: **221 passed**, 1 warning deprecation Starlette/AnyIO; selesai sekitar 2 detik.
- Frontend: **production build berhasil**; lint berhasil dengan 1 warning `react(set-state-in-effect)` di Dashboard.
- Browser: membuka kartu, rincian riset, filter label, audit, penolakan pipeline ketika Sectors nonaktif, dan scan saat snapshot offline tidak tersedia.
- Tampilan diperiksa pada 375, 768, dan 1440 piksel. Pada sampel yang dibuka tidak terlihat overflow horizontal; audit berupa JSON panjang sangat sulit dibaca pada ponsel.
- Empat observasi tambahan direproduksi dengan model/sumber palsu: antrean macet, status selesai sebelum publikasi, dokumen lintas tahun tergabung, dan event progres terlambat.

Belum diperiksa: integrasi Sectors live, inference Ollama live, kualitas label terhadap dataset berlabel manusia, pengujian beban/multi-worker, audit aksesibilitas penuh, dan Core Web Vitals. Tidak ada baseline screenshot sehingga status **visual regression: INCONCLUSIVE**. Kelulusan unit test tidak membuktikan kualitas label model.

**Temuan berurutan menurut prioritas**

1. **P1 — Kandidat gagal bisa menghalangi seluruh antrean berikutnya.**

   [scan.py:244](/Users/yhnswp/Desktop/signalgate/backend/app/scan.py:244) selalu membaca berkas berdasarkan nama hash. Hanya `completed` dan `needs_review` yang dilewati. Kandidat `insufficient_evidence`, `needs_document`, atau `model_unavailable` terus mengambil jatah `limit` pada setiap run.

   Reproduksi: siapkan empat kandidat berurutan; tiga pertama selalu menghasilkan `insufficient_evidence`, kandidat keempat dapat selesai. Dua run dengan limit 3 sama-sama hanya memproses `AA00`, `AA01`, `AA02`; `AA03` tetap pending. Pada endpoint, hasil gagal yang sama juga bisa disimpan berulang ke dashboard.

   Perbaikan: prioritaskan kandidat yang belum pernah dicoba; simpan `attempt_count`, `last_attempt_at`, dan `next_retry_at`; beri jeda/batas retry per jenis kegagalan. Kriteria selesai: kandidat baru tetap mendapat giliran walau kandidat sebelumnya terus gagal.

2. **P1 — Hasil dapat selesai diriset tetapi tidak pernah muncul di dashboard.**

   [scan.py:257](/Users/yhnswp/Desktop/signalgate/backend/app/scan.py:257) menulis status final antrean sebelum `yield`. Baru kemudian [main.py:100](/Users/yhnswp/Desktop/signalgate/backend/app/main.py:100) menyimpan hasil ke SQLite. Jika proses berhenti atau penulisan database gagal di antaranya, run berikutnya melewati kandidat karena sudah `completed`/`needs_review`.

   Reproduksi: ambil satu hasil dari generator lalu tutup sebelum penyimpanan dashboard. Status antrean sudah `completed`; percobaan berikutnya menghasilkan nol item. Artefak kasus bisa tetap ada, tetapi publikasinya ke dashboard tidak dipulihkan otomatis. Jalur CLI yang berbagi direktori antrean juga dapat membuat endpoint melewati hasil yang belum pernah dipublikasikan ke database.

   Perbaikan: pisahkan status selesai riset dan selesai publikasi, simpan hasil dengan ID kandidat yang unik, lalu lakukan konfirmasi publikasi setelah commit. Sediakan pemulihan publikasi dari artefak yang sudah ada. Kriteria selesai: simulasi gagal commit lalu retry menghasilkan tepat satu kartu tanpa inference ulang yang tidak perlu.

3. **P1 — PDF dari aksi berbeda dapat masuk ke bukti kasus yang sama.**

   [scan.py:141](/Users/yhnswp/Desktop/signalgate/backend/app/scan.py:141) menghubungkan artikel dengan semua pengumuman yang ticker dan salah satu kata kuncinya cocok. Tidak ada pemeriksaan tanggal, nomor pengumuman, atau identitas aksi. `published_at` kandidat scan juga diisi kosong.

   Reproduksi: artikel rights issue 2026 dengan ticker TEST mendapat dua lampiran, yaitu rights issue 2024 dan 2026. Keduanya masuk `pdf_urls`. [engine.py:202](/Users/yhnswp/Desktop/signalgate/backend/app/research/engine.py:202) memeriksa identitas emiten dan keterbacaan PDF; kecocokan aksi/tanggal hanya dicatat sebagai kebutuhan review, bukan syarat yang menahan penyelesaian.

   Dampak: kutipan bisa benar-benar ada di dokumen tetapi menjelaskan aksi yang berbeda. Reproduksi membuktikan pencampuran kandidat dokumen, belum membuktikan model menghasilkan label salah pada kasus nyata.

   Perbaikan: ikat bukti dengan tanggal aksi, nomor surat/pengumuman, dan hubungan lampiran. Saat asosiasi ambigu, minta review dan jangan nyatakan asosiasi selesai. Kriteria selesai: dua rights issue berbeda dari emiten yang sama tidak saling memakai lampiran tanpa hubungan yang eksplisit.

4. **P2 — Progres real-time tidak menunjukkan pekerjaan yang sedang dilakukan.**

   [orchestrator.py:39](/Users/yhnswp/Desktop/signalgate/backend/app/pipeline/orchestrator.py:39) baru menerbitkan `research` sesudah semua pembacaan model selesai. Event `validate` hanya muncul untuk masalah numerik, bukan saat validator model mulai. [RunProgress.tsx:46](/Users/yhnswp/Desktop/signalgate/frontend/src/components/RunProgress.tsx:46) mengambil indeks tahap tertinggi sepanjang run.

   Reproduksi backend dua kasus: ketika model mengerjakan kasus pertama, event yang tersedia baru `sense`; ketika model mengerjakan kasus kedua, event terakhir adalah `research` dan `gate` milik kasus pertama. UI karena itu bertahan di tahap kepatuhan selama model membaca kasus berikutnya. Tahap yang tidak pernah dijalankan juga dapat ditandai selesai hanya karena indeksnya lebih rendah. Hitungan kasus selesai naik pada event gate, sebelum commit kartu.

   Perbaikan: kirim event awal/akhir setiap operasi dengan `run_id`, `case_id`, peran model, dan nomor kasus. Perbarui status dari operasi aktif; tandai kasus selesai setelah commit. Tampilkan hanya tahap yang berlaku pada jalur scan atau pipeline tersebut.

5. **P2 — Refresh halaman atau koneksi POST putus tidak memiliki pemulihan run.**

   [Dashboard.tsx](/Users/yhnswp/Desktop/signalgate/frontend/src/pages/Dashboard.tsx:31) menyimpan status run hanya di state React. SSE dipasang hanya selama state itu aktif. Backend tidak menyediakan status job yang bisa dibaca ulang; stream juga tidak menyimpan riwayat untuk reconnect. Reload menghapus tampilan progres meskipun backend masih bekerja. Klik run kembali dapat berakhir 409.

   Temuan ini berdasarkan penelusuran kode; skenario refresh saat inference panjang belum diuji live.

   Perbaikan: buat job tersimpan dengan ID dan status; POST mengembalikan ID, dashboard dapat mengambil status dan hasil setelah reload, dan SSE mengikuti job tersebut. Untuk aplikasi lokal, job ringan dengan SQLite sudah dapat menjadi langkah awal.

6. **P2 — Pesan error menyembunyikan tindakan yang perlu dilakukan pengguna.**

   [client.ts:7](/Users/yhnswp/Desktop/signalgate/frontend/src/api/client.ts:7) membuang isi respons error FastAPI. Di browser, pipeline saat Sectors dinonaktifkan menampilkan `SignalGate API /pipeline/run failed: 400 — cek daftar di bawah, hasilnya mungkin sudah tersimpan.` Penjelasan backend tentang mode hemat tidak ditampilkan; pada kasus ini run memang ditolak sebelum mulai.

   Perbaikan: baca `detail` dari JSON error dengan fallback untuk respons non-JSON; bedakan konfigurasi, run masih berjalan, kegagalan sumber, dan koneksi putus. Pesan “mungkin sudah tersimpan” hanya cocok untuk hasil request yang tidak diketahui. Tambahkan status kesiapan Sectors/Ollama di UI agar kegagalan konfigurasi terlihat sebelum tombol ditekan.

7. **P2 — Alur pemeriksaan manual belum memberi informasi yang cukup di kartu.**

   [EventCard.tsx:61](/Users/yhnswp/Desktop/signalgate/frontend/src/components/EventCard.tsx:61) hanya menampilkan jumlah `issues`. Sampel IDEA menampilkan “Masih ada 2 hal yang perlu diperiksa”, tetapi tidak menyebut kutipan yang gagal ditemukan maupun perbedaan pembacaan independen. Informasi itu ada di JSON audit. Fakta, kutipan, dan status dukungan validator juga belum disajikan sebagai rincian yang mudah diperiksa.

   Perbaikan: tampilkan daftar alasan review, fakta yang diterima/ditolak, kutipan sumber, nomor halaman, dan status setiap pembaca. Kelompokkan sumber per dokumen, bukan satu baris panjang untuk tiap halaman. Keluarkan bukti `kind=input` dari daftar bukti pendukung atau tandai terpisah; input sendiri tidak diterima oleh verifikasi fakta backend.

   Kriteria selesai: pengguna bisa menjawab “mengapa hasil ini inconclusive?” dan menelusuri kutipannya dari kartu, tanpa membuka JSON mentah. Ini perbaikan UX yang paling bernilai setelah tiga P1.

8. **P2 — Ringkasan scan mencampur penemuan baru, antrean lama, dan kandidat yang sudah selesai.**

   [main.py:91](/Users/yhnswp/Desktop/signalgate/backend/app/main.py:91) meriset seluruh antrean tersimpan, sedangkan `candidates_found` berasal dari discovery saat ini. Karena itu jumlah “kasus diriset dari N kandidat” bisa tidak sebanding. [Dashboard.tsx:25](/Users/yhnswp/Desktop/signalgate/frontend/src/pages/Dashboard.tsx:25) juga menganggap tidak adanya hasil selalu terkait PDF jika kandidat ditemukan. Padahal artikel tanpa PDF sudah dapat diriset dan kandidat yang sudah selesai sengaja dilewati.

   Contoh berdasarkan kode: tiga kandidat dengan PDF sudah selesai ditemukan ulang; hasil run nol; UI menyatakan tiga kandidat ditemukan tetapi nol tanpa PDF sehingga tidak bisa diriset. Jika sebagian sumber gagal namun ada hasil, cabang sukses justru menyembunyikan daftar kegagalan.

   Perbaikan: respons terstruktur berisi `discovered`, `new`, `already_processed`, `pending`, `retried`, `processed`, dan `failed_sources`. Tampilkan “semua kandidat sudah pernah diproses” saat sesuai; pertahankan peringatan sumber parsial meskipun ada hasil.

9. **P2 — Hasil lama tidak dapat dicari setelah batas daftar terlampaui.**

   [routes.py:42](/Users/yhnswp/Desktop/signalgate/backend/app/api/routes.py:42) membatasi events menjadi 100 secara default, maksimum 500, tetapi tidak menyediakan offset/cursor atau filter server. Dashboard memfilter label hanya dari 100 kartu yang diambil. Audit dibatasi 200 tanpa pagination.

   Dampak berdasarkan kode: bila seluruh red flag lebih lama daripada 100 hasil terbaru, tab red flag menampilkan nol walaupun hasilnya masih ada di database. Batas payload sudah baik, tetapi perlu akses ke halaman berikutnya dan arti hitungan yang jelas.

   Perbaikan: pagination/cursor, filter ticker/label/tanggal di server, jumlah total sesuai filter, serta label “100 hasil terbaru” bila memang hanya menampilkan subset. Verifikasi dengan >100 hasil dan label target hanya pada halaman lama.

10. **P2 — Timestamp audit kehilangan zona waktu setelah melewati SQLite.**

    [routes.py:68](/Users/yhnswp/Desktop/signalgate/backend/app/api/routes.py:68) mengirim `created_at.isoformat()` dari datetime hasil pembacaan SQLite tanpa informasi zona. Respons QA nyata berbentuk `2026-09-16T07:03:52.379197`, sedangkan timestamp riset/SSE menggunakan `+00:00`.

    Frontend memanggil `new Date(created_at)`, sehingga timestamp tanpa offset ditafsirkan sebagai waktu lokal. Pada browser WIB, waktu UTC 07:03 dapat ditampilkan sebagai 07:03, padahal seharusnya 14:03. Pergeseran WIB ini merupakan konsekuensi format; browser QA tidak diubah zona waktunya untuk reproduksi visual.

    Perbaikan: serialisasikan seluruh timestamp dalam UTC dengan `Z`/`+00:00`, lalu lakukan konversi lokal di UI. Tampilkan tanggal dan zona waktu pada audit, bukan hanya jam. Uji round-trip SQLite → API → browser WIB.

**Perbaikan berikutnya setelah masalah utama**

- Pisahkan kondisi daftar benar-benar kosong, filter tidak memiliki hasil, sedang memuat, dan gagal memuat. Browser menunjukkan instruksi “Belum ada event — jalankan Scan” ketika filter red flag kosong meskipun tab Semua memiliki satu event.
- Perbarui dokumentasi: [README.md:208](/Users/yhnswp/Desktop/signalgate/README.md:208) menyebut kandidat tanpa PDF dilewati, sedangkan kode dan tes sudah mendukung artikel; [ARCHITECTURE.md:221](/Users/yhnswp/Desktop/signalgate/docs/ARCHITECTURE.md:221) menyebut PDF belum didukung, padahal modul PDF sudah ada.
- Rapikan audit menjadi ringkasan yang dapat diperluas; tampilkan JSON mentah sebagai opsi teknis. Tampilan ponsel tidak overflow pada sampel, tetapi satu hasil riset menjadi kolom teks sangat panjang.
- Tambahkan status terpilih `aria-pressed` pada filter, `aria-expanded` pada tombol audit, landmark `main`, dan pengumuman error/status yang sesuai. Periksa ulang kontras teks kecil yang diberi opacity. Ini hasil inspeksi kode/visual, bukan sertifikasi aksesibilitas.
- Buat paket demo offline dengan bukti tetap dan label acuan manusia. Ukur precision/recall per label, proporsi inconclusive, dukungan kutipan, dan waktu proses. Jangan menyimpulkan ketepatan model dari jumlah unit test atau kesepakatan antarmodel.
- Tunda penambahan model/fitur besar sampai pemulihan job, identitas bukti, dan alur review manual berfungsi. Tiga pembaca tidak menyelesaikan masalah antrean atau dokumen yang salah terasosiasi.

**Urutan pengerjaan yang disarankan:** antrean dan publikasi hasil → asosiasi dokumen → rincian review → progres dan pemulihan job → pesan/riwayat/timestamp → evaluasi model dan penyempurnaan tampilan.

Observasi backend dapat diulang menggunakan [skrip reproduksi QA](/Users/yhnswp/Desktop/signalgate/docs/qa_2026_09_16_reproduce.py). Dari direktori backend, jalankan `.venv/bin/python ../docs/qa_2026_09_16_reproduce.py`. Skrip memakai direktori sementara dan model palsu; output mencatat perilaku saat review, bukan menandai bug tersebut sebagai hasil yang benar untuk tes regresi.
